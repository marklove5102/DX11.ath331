#pragma once

////////////////////////////////////////////////////////////////////////////////////////////////////
// @brief Game을 동작 시키는 대표 클래스
////////////////////////////////////////////////////////////////////////////////////////////////////
class Game
{
public:
	/// 생성자
	Game();

	/// 소멸자
	~Game();

public:
	/// 클래스를 초기화한다.
	void Init( HWND hwnd );

	/// 업데이트 한다.
	void Update();

	/// 렌더링을 한다.
	void Render();

private:
	/// 윈도우 핸들
	HWND m_hwnd;

	/// 윈도우 창 넓이
	uint32 m_width;

	/// 윈도우 창 높이
	uint32 m_height;

// DX
private:

};

